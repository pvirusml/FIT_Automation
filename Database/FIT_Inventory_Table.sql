CREATE TABLE [dbo].[Model_Code] (
    [Id]              INT          NOT NULL,
    [Model_Code_Name] VARCHAR (50) NOT NULL,
    [Production_Name] VARCHAR (50) NOT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

